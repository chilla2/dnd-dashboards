{% extends 'base.html.twig' %}

{% block title %}Encounter{% endblock %}

{% block body %}
    <h1>Encounter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ encounter.id }}</td>
            </tr>
            <tr>
                <th>Characters</th>
                <td>{{ encounter.fighters ? encounter.fighters|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('encounter_index') }}">back to list</a>

    <a href="{{ path('encounter_add_fighters', {'id': encounter.id}) }}">edit</a>

    {{ include('encounter/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('dash_delete', {'id': dash.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dash.id) }}">
    <button class="btn">Delete</button>
</form>
